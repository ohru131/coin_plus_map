# GitHub Pages 自動デプロイ検証記録

## 2026-08-20

`main`へのpushにより、GitHub Pagesの自動デプロイワークフローが実行された。実行ID `32340458909` では、店舗データ生成、静的サイトのビルド、Pagesアーティファクトのアップロード、および公開処理がすべて成功した。

しかし、公開URL `https://ohru131.github.io/coin_plus_map/` ではアプリケーション側の「404 Page Not Found」画面が表示された。GitHub Pages自体ではなく、リポジトリ配下のベースパスをクライアント側ルーターが解決する処理に課題が残っているため、公開前に修正・再検証を継続する。

公開版を調査したところ、ブラウザのパスは`/coin_plus_map/`であり、JavaScriptアセットと店舗データマニフェストは同じリポジトリ配下パスから読み込まれていた。静的アセットのベースパスは正しいため、原因はWouterのルート照合に限定して修正する。

WouterのベースパスをViteの`BASE_URL`に合わせて再デプロイした後、キャッシュ回避用クエリを付けた公開URLで確認した。COIN+ストアマップ本体、11,093件の店舗データ、ジャンル件数、Leaflet地図、OpenStreetMapの帰属表示が正常に表示された。通常URLに一時的に古いアセットが残る場合は、ブラウザのキャッシュまたはGitHub PagesのCDN伝播を待って再読込する。
